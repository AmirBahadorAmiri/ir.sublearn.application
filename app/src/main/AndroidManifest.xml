<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:name=".AppManager"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:extractNativeLibs="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.SubLearn">

        <activity
            android:name=".views.activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".views.activities.LogisterActivity" />
        <activity android:name=".views.activities.MainActivity" />
        <activity android:name=".views.activities.DictionaryHistoryActivity" />
        <activity android:name=".views.activities.ShowWordActivity" />
        <activity android:name=".views.activities.StarWordsActivity" />
        <activity android:name=".views.activities.SettingsActivity" />
        <activity android:name=".views.activities.ConversationActivity" />
        <activity android:name=".views.activities.TranslateHistoryActivity" />
        <activity android:name=".views.activities.ChangeLanguageActivity" />
        <activity android:name=".views.activities.SongsExplorerActivity" />
        <activity android:name=".views.activities.SongPlayerActivity" />
        <activity android:name=".views.activities.ProfileActivity" />
        <activity android:name=".views.activities.SeriesActivity" />
        <activity android:name=".views.activities.SeriesExplorerActivity" />
        <activity android:name=".views.activities.MoviesExplorerActivity" />
        <activity android:name=".views.activities.MoviePlayerActivity" android:screenOrientation="sensorLandscape" />

    </application>

</manifest>