<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:name="ir.sublearn.AppManager"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.SubLearn">

        <activity
            android:name="ir.sublearn.views.activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="ir.sublearn.views.activities.LogisterActivity" />
        <activity android:name="ir.sublearn.views.activities.MainActivity" />
        <activity android:name="ir.sublearn.views.activities.DictionaryHistoryActivity" />
        <activity android:name="ir.sublearn.views.activities.ShowWordActivity" />
        <activity android:name="ir.sublearn.views.activities.StarWordsActivity" />
        <activity android:name="ir.sublearn.views.activities.SettingsActivity" />
        <activity android:name="ir.sublearn.views.activities.ConversationActivity" />
        <activity android:name="ir.sublearn.views.activities.TranslateHistoryActivity" />
        <activity android:name="ir.sublearn.views.activities.ChangeLanguageActivity" />
        <activity android:name="ir.sublearn.views.activities.SongPlayerActivity" />
        <activity android:name="ir.sublearn.views.activities.ProfileActivity" />
        <activity android:name="ir.sublearn.views.activities.SeriesActivity" />
        <activity android:name="ir.sublearn.views.activities.SeriesExplorerActivity" />
        <activity android:name="ir.sublearn.views.activities.MoviesExplorerActivity" />
        <activity
            android:name="ir.sublearn.views.activities.MoviePlayerActivity"
            android:screenOrientation="sensorLandscape" />

    </application>

</manifest>